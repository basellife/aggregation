# graphql server for newsapi calls

Start


```bash
git clone https://github.com/newshost/server
cd newshost
npm install
npm start
```

Then open [http://localhost:8082/graphiql](http://localhost:8082/graphiql)

When you paste this on the left side of the page:

```graphql
{
  testString
}
```
```
